## Portal EDIGA 

Aplicación Web en React para el Portal EDIGA

## Setup

Clonar este repositorio. Previamente instalar `node` and `npm` globalmente.  

Instalar dependencias:

`npm install`   

Levantar el servidor:

`npm start`
